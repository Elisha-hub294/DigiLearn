import { Image } from "expo-image"; import { LinearGradient } from "expo-linear-gradient"; import { StyleSheet, Text } from "react-native"; import Animated, { FadeInRight } from "react-native-reanimated"; import type { Interest } from "./profileData";
export function InterestCard({item}:{item:Interest}){return <Animated.View entering={FadeInRight.duration(320)} style={s.wrap}><LinearGradient colors={item.colors} style={s.card}><Image source={item.image} style={s.image} contentFit="contain"/><Text numberOfLines={1} style={s.title}>{item.title}</Text></LinearGradient></Animated.View>}
const s=StyleSheet.create({wrap:{width:112},card:{height:94,borderRadius:16,padding:9,alignItems:"center",justifyContent:"center"},image:{width:48,height:48},title:{color:"#fff",fontSize:12,fontWeight:"800",marginTop:2,textAlign:"center"}});
